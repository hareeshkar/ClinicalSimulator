import Foundation

/// A simple struct to define the core idea for a new clinical case.
struct CaseTemplate {
    let title: String
    let specialty: String
    let difficulty: String
    let chiefComplaint: String
    let finalDiagnosis: String
    let iconName: String // The SF Symbol for the category card
}
